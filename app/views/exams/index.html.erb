<div class="contenedor-receta">
  <h1 style="position: relative; left: 4rem;">Examenes Médicos</h1>
</div>
<div class="contenedor mt-5 vh-100">

<div class="card mt-5" style="width: 50rem;padding-top: 0rem;position: relative;top: -13rem;">
  <div class="row"style="display: flex;position: relative;right: 16rem;top: 2rem;">
        <h2 style="position: relative;left: 2rem;top: 2.4rem;">Examenes</h2>
    <div class="button-exam" style="position: relative;right: -33rem;top: -0.5rem;">
        <%= link_to "+", new_recipe_path, class:"btn btn-primary mb-5" %>
    </div>
  </div>

  <div class="row">
    <table class="table">
    <thead>
      <tr>
        <th scope="col">Fecha</th>
        <th scope="col">Nombre </th>
        <th scope="col">Lugar</th>
        <th scope="col">Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% @exams.each do |exam| %>
      <tr>
        <th scope="row"><%= exam.date %></th>
        <td><%= exam.name %></td>
        <td><%= exam.place %></td>
        <td><%= link_to "eliminar", exam_path(exam), data: {turbo_method: :delete, turbo_confirm: "Desea eliminar el elemento de manera permanente?"}  %>  |  <%= link_to "editar", edit_exam_path(exam) %>  |  <%= link_to "ver", exam_path(exam) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
</div>
